# PyUVM-on-cocotb tier for chi_to_cxl_bridge. Pins are loose on purpose; CI
# installs these against the apt/OSS verilator + icarus images. Aligned with
# ../ucie2-pipe7-bridge/dv/pyuvm/requirements.txt.
cocotb>=1.9
pyuvm>=3.0
# Functional coverage (make fcov) AND the constrained-random sequence
# (seq_lib.chi_seq_lib.ChiReqRandom uses cocotb_coverage.crv), so it is required
# by the whole pyuvm tier, not just fcov.
cocotb_coverage
